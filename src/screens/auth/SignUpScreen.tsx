import { Text, SafeAreaView } from 'react-native';

const SignUpScreen = () => {
  return (
    <SafeAreaView>
      <Text>Sign Up Screen</Text>
    </SafeAreaView>
  );
};

export default SignUpScreen;
